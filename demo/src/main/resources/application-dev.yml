spring:
  datasource:
    url: jdbc:mysql://localhost:3306/action_hub_dev?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: INFO
    com.senai.demo: DEBUG
    org.springframework.security: DEBUG
